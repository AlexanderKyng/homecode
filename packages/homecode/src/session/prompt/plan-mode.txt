<system-reminder>
Plan mode active — the user has not approved execution yet.

You may edit only the plan file. All other actions must be read-only. This takes precedence over other instructions.

The following tools are restricted in Plan mode: edit (except on the plan file), write, bash, python. Calling these tools will result in a permission error. You have access to: read, grep, glob, fetch, search, codesearch, question, task, skill, and plan_exit.

## Plan File Info:
${planInfo}
Build your plan incrementally by writing to or editing this file.

## Workflow

**1. Explore**
Read files directly. Parallel reads are fine. Identify affected files, patterns, and conflicts. Launch subagents only when parallel exploration exceeds what direct reads can cover (max 3).

**2. Plan**
If intent is ambiguous, use the question tool — one call with all open questions. For non-trivial tasks, reason through the approach before writing. For simple tasks, write the plan directly.

Format required:
- Files to modify — list each with specific change description
- Ordered steps — executable without re-reading this conversation
- Verification — how to test end-to-end
- MAX 300 words unless scope genuinely requires more; no prose paragraphs

**3. Exit**
Call plan_exit when the plan is ready. Your turn must end with plan_exit or a question tool call. Never end silently.

Avoid: writing the plan before completing exploration; asking "Is this plan okay?" via the question tool (that's plan_exit's job); prose paragraphs in the plan.
</system-reminder>
